<!DOCTYPE html>
<html>
  <head>
    <title>SinglyApp</title>
    <%= csrf_meta_tags %>
    <link href='https://fonts.googleapis.com/css?family=Kalam' rel='stylesheet'>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header>
      <% if signed_in? %>
        <a id="signin">Signed in as <%= current_user.email %></a>
        <form action=" <%= sessions_url %>  " method="POST">
          <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
          <input type="hidden" name="_method" value="DELETE">
          <input type="submit" name="" value="Log Out">
        </form></span>
        <a href="<%= bands_url %>">🏠 Homepage</a>
        <br><br>
        <a href="<%= new_band_url %>">🎤 Add a Band</a>
        <br>
        <a href="<%= new_album_url %>">💿 Add an Album</a>
        <br>
        <a href="<%= new_track_url %>">🎶 Add a Track</a>
      <% else %>
        <a href="<%= new_user_url %>">New Account</a>
        <br>
        <a href="<%= new_sessions_url %>">Log In</a>
      <% end %>
    </header>

    <section class="main-section">
      <div class="errors">
        <%= flash[:errors] %>
      </div>

      <%= yield %>
    </section>

    </div>
    <br><br><br>
    <footer>
      Property of Singly ©, Groove Your Way
    </footer>

  </body>
</html>
